{
  "name": "steam-app-store",
  "version": "1.0.0",
  "scripts": {
    "install-all": "npm install --prefix src/server && npm install --prefix src/client",
    "start-server": "npm start --prefix src/server",
    "start-client": "npm run dev --prefix src/client"
  }
}